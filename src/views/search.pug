extends base
include mixins/video

block content 
    form(method="GET").search__form
        input(placeholder="Search By Title", type="text", name="keyword")
        button
            i.fas.fa-search

    div.video-grid
        each video in videos 
            +video(video)
        else 
            span.empty_message 비디오를 찾을수 없습니다.